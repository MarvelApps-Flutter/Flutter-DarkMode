import 'package:flutter/material.dart';

class ProgressModel {
  String title;
  double progress;
  Color color;
  ProgressModel({
    required this.title,
    required this.progress,
    required this.color
  });
}
